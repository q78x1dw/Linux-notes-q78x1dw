# Exercise 17

## how to make it crash

the bug in using `strncpy` had been solved


## problems

- [x] `die` needs `conn` as a param
- [] receive params as `MAX_DATA` and `MAX_ROWS`
- [] adding more operations to db such as `find`
- [x] C how to pack structs? why file is the corresponding size;
- [] **?** add some fields to `Address` to make them able to be searched
- write a shell script to automatically test
