# variables

1. global   by default all vars are global
2. local    use`local` to declare ; lasts from `local` to dhe end of dhe statement block 
3. field in a table

like c , local vars override globals with same name.

## assignments

calculate rightvalues then assign

assigning multiple vars simutanously
`x, y = y, x -- swap x and y`

rvalues not enough : last lvalues are assigned `nil`

## index

`[]` as in C. `t['string']` equivalent to `t.string`
